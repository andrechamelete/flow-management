import { Component } from '@angular/core';

@Component({
  selector: 'app-aging',
  imports: [],
  templateUrl: './aging.component.html',
  styleUrl: './aging.component.scss'
})
export class AgingComponent {
  //cards sem finishedAt
  // expor cards por stage (eixo x) e por tempo (now() - createdAt) (eixo y)
  //expor percentis de tempo por stage acumulado 
}
